# Separate communication History and reporting

History recording, local prediction learning, local operational logging, and anonymous feature reporting are separate controls because they expose different information for different purposes. History is local and enabled by default; prediction learning and both forms of reporting require opt-in. Disabling History stops new Message text from being persisted, and clearing learned communication data removes both History and the prediction model after confirmation.

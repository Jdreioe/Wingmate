# Fall back to system speech

When selected Azure speech is unavailable for uncached text, Wingmate automatically tries an available system voice and shows a visible, non-blocking explanation. If no fallback can speak, Wingmate preserves the active Message and offers retry; a failed or stopped playback never clears it.

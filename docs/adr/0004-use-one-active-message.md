# Use one active Message

Typing and Screens share one active Message made from structured parts that retain display text, spoken text, and optional visual metadata. The active and Held Messages survive workspace changes, app closure, process recreation, and device restart until explicitly cleared. The Message remains after speech by default; an optional clearing policy applies only after successful full-Message playback.

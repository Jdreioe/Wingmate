# Keep communication data local-first

Wingmate stores communication content on the Communicator's device and uses explicit backup, restore, and vocabulary transfer instead of synchronization. This avoids introducing accounts, conflict handling, and a remote store for sensitive communication data without a concrete need. Unencrypted backups exclude speech-service credentials, editing-access verifiers, analytics identifiers, and derived caches.
